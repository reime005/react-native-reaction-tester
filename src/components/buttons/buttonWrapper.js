import React from 'react';
import {
  Button
} from 'react-native';
import {
  connect
} from 'react-redux';

const ButtonWrapper = (props) => (
  <Button {...props}/>
)

export default ButtonWrapper;